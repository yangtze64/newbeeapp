import React from 'react';
import {SafeAreaView, StyleSheet, View, Text, StatusBar} from 'react-native';

export default class TaskInfo extends React.Component {
  render() {
    return (
      <View>
        <Text>this is TaskInfo</Text>
      </View>
    );
  }
}
